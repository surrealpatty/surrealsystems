# .env.example — copy to .env for local development (do NOT commit .env)

NODE_ENV=development
PORT=10000

# Security / Auth
# Use JWT_SECRETS in production for rotation.
JWT_SECRET=your_jwt_secret_here
# JWT_SECRETS=secret_current,secret_previous

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# Database (local dev placeholders)
DB_HOST=db.deusbwbqgwcwvaemhutt.supabase.co
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/codecrowds

# Debug / Perf tuning
SQL_LOG=false
DB_POOL_MAX=10
DB_POOL_MIN=2
DB_POOL_IDLE=10000
DB_POOL_ACQUIRE=30000

# Schema sync (dev only)
DB_ALTER=true

# Frontend
FRONTEND_URL=http://localhost:3000

# Payments / Stripe (leave blank until configured)
ENABLE_PAYMENTS=false
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
STRIPE_WEBHOOK_SECRET=
