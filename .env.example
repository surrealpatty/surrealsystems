# --- App
NODE_ENV=development
PORT=10000

# --- Security
# Prefer JWT_SECRETS (comma-separated) in production to support rotation.
# Example: JWT_SECRETS="secret-v2,secret-v1"
# You may keep JWT_SECRET for compatibility, but use JWT_SECRETS when possible.
JWT_SECRET=change-me-to-32plus-random-chars
# JWT_SECRETS=your_new_secret,old_secret

CORS_ALLOWED_ORIGINS=http://localhost:3000
RATE_LIMIT_MAX=100

# --- Database (choose ONE pattern)
# A) Single URL (prod recommended)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=require

# B) Split vars (local dev)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=codecrowds

# --- Debug/Perf
SQL_LOG=false
DB_POOL_MAX=10
DB_POOL_MIN=2
DB_POOL_IDLE=10000
DB_POOL_ACQUIRE=30000

# --- Schema sync (dev only)
DB_ALTER=false

# --- Frontend / app URLs
FRONTEND_URL=http://localhost:3000

# --- Payments / Stripe
# Keep payments disabled by default in dev. To exercise Stripe logic set ENABLE_PAYMENTS=true
ENABLE_PAYMENTS=false
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
STRIPE_WEBHOOK_SECRET=

# --- Notes
# - Do NOT commit real secrets. Use environment variables in your CI / hosting provider or a secrets manager.
# - JWT_SECRET should be at least 32 characters (random). For rotation use JWT_SECRETS (comma-separated, first is signing).
# - If you use DATABASE_URL in production, include `?sslmode=require` or rely on the DB provider's instructions for SSL.
